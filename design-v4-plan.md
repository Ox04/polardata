# 디자인 V4 수정 계획

## 방향

V3의 정보 패널 구조를 버리고, 해빙 데이터를 화면의 중심에 놓는다.
지도는 화면의 약 70~80%를 차지하는 얕은 3D 데이터 부조로 표현한다.

핵심 원칙:

- 데이터가 배경이 아니라 작품의 주인공이 되게 한다.
- 3D는 연령 차이를 돕는 정도로만 사용한다.
- 높은 구조물을 실제 해빙 두께로 오해하지 않게 명시한다.
- 상시 패널은 제거하고 추세 그래프는 숫자 뒤의 반투명 오버레이로 사용한다.

## 화면 구성

```text
┌──────────────────────────────────────────────────────────────┐
│ 2013 / 11주차                                                │
│                                                              │
│                                                              │
│                 크게 떠 있는 3D 해빙 부조                    │
│           젊은 얼음은 낮게 · 오래된 얼음은 높게             │
│                                                              │
│                                                              │
│ 해빙 연령              ~~~ 여름을 견딘 얼음 19.9% ~~~        │
│ 높이도 연령 표현 · 실제 두께 아님          NSIDC-0611        │
└──────────────────────────────────────────────────────────────┘
```

연도와 핵심 수치는 화면 가장자리에 직접 놓고 박스, 구분선과 큰 패널은
사용하지 않는다.

## 3D 표현

- 0~1년 얼음은 낮은 파란 층으로 표현한다.
- 연령이 높을수록 조금씩 높이고 짙은 청색에서 밝은 청백색으로 바꾼다.
- 육지는 아주 낮은 짙은 회색 부조로 남겨 위치 기준을 제공한다.
- 카메라는 거의 위에서 내려다보되 약 20~25도 기울여 높이가 살짝 보이게 한다.
- 카메라 위치와 조명은 연도 전체에서 고정한다.
- 실제 두께값은 사용하지 않으며 높이는 정규화된 해빙 연령이다.
- 최종 영상의 지도는 지표 계산에 사용한 공간 범위를 임의로 잘라내지 않는다.
- 제작용 다운샘플링은 표시 해상도만 줄이며 연령 구간과 지표 계산에는 적용하지 않는다.

입체감을 과장하면 오래된 얼음이 실제로 매우 두껍다는 잘못된 인상을 줄 수
있으므로 가장 높은 층도 지도 폭에 비해 낮게 유지한다.

## 정보 구조

### 본편에 계속 표시

- `연도 / 11주차`
- `밝고 높은 블록일수록 오래된 얼음`을 명시한 연령 범례
- 핵심 수치 뒤의 축 없는 반투명 추세선
- `높이도 연령 표현 · 실제 두께 아님`
- 데이터 출처

### 필요한 순간에만 표시

- `여름을 견딘 얼음` 비율
- 환경·생태 설명

추세 그래프는 별도 패널로 분리하지 않는다. 핵심 수치를 그래프 영역의 좌측
상단에 두고 실제 선 그래프는 그 아래에서 시작한다. 값은 `2년차 이상 격자 /
전체 해빙 격자`로 정의한다. 우측 가장자리에는 과장을 피하기 위해
`50%`와 `0%` 척도를, 아래쪽에는 `1984—2024`를 표시한다. 선 아래는
반투명 청색으로 채우고 현재 연도까지의 선과 점만 밝게 표시한다.
데이터 출처는 그래프 우측 가장자리와 같은 선에 맞춰 화면 맨 아래에 둔다.

## 움직임

- 최종 영상은 블록형 표면을 사용한다.
- 카메라와 지도 방향은 전 구간에서 고정한다.
- 각 연도 사이에 블록 높이와 출현·소멸을 보간한 다섯 개의 실제 3D 중간
  상태를 렌더링한다.
- 연령 색은 혼합하지 않고 다섯 개의 범례 색만 사용한다.
- 인접한 3D 상태를 8단계로 시간 보간해 60fps에서 부드럽게 연결한다.
- 화면 전체 페이드와 형태를 추정하는 생성형 모핑은 사용하지 않는다.
- 연도별 정지 구간은 없앤다.
- 색, 높이와 수치는 같은 속도로 변한다.
- 화면 전체 페이드, 검은 화면, 흐림과 생성형 모핑 효과는 사용하지 않는다.
- 연도·범례·출처와 그래프 글자는 매 프레임 다시 그려 선명하게 유지한다.

## 환경 메시지

별도 메시지 슬라이드는 만들지 않는다. 작품 설명서에는 직접적인 생태계 약화
단정을 피하고 다음 문장을 사용한다.

> 해빙은 북극 생물이 살아가는 공간이자 바다와 대기를 잇는 경계다.  
> 오래된 얼음의 감소는 그 환경의 구조가 바뀌고 있음을 보여준다.

설명서에는 `NOAA Arctic Report Card 2024`를 근거로 표시한다.

## 예상 결과

- 데이터 지도 점유율: 기존 약 45% → 약 70~80%
- 상시 정보량: 연도·범례·출처만 유지
- 패널형 UI: 제거
- 그래프: 우측 하단 수치와 결합한 오버레이
- 3D 높이: 해빙 연령을 위한 낮은 부조

## 목업

- `outputs/design-v4/mockup-2013.png`
- `outputs/design-v4/map-3d-2013.png`
- `outputs/design-v4/compare-v3-v4.png`

목업 승인 후 동일한 장면 구조를 1984~2024년 전체 애니메이션에 적용한다.

## 최종 영상

- 작은 북극 원형 지도에서 같은 좌표의 1984년 데이터 화면으로 확대되는
  약 3.5초 도입부를 사용한다.
- 카드형 비교 슬라이드는 사용하지 않는다.
- 마지막에는 같은 좌표의 1984년과 2024년 지도를 수직선으로 나눠 약 5초간
  직접 비교한다.
- 1920×1080, 60fps, 44.5초, H.264 MP4로 제작한다.
- 출력: `outputs/final/polar-memory-final-v6.mp4`
