# 디자인 V6 과학·출력 검증

- 검증일: 2026년 7월 31일
- 데이터: NSIDC-0611, EASE-Grid Sea Ice Age, Version 4.1
- 사용 범위: 1984~2024년, 각 연도 제11주
- 판정: **최종 영상 통과**

## 결론

V6의 연령 구간, 2013년 표시값 `19.9%`, 추세 그래프와 3D 높이의 데이터
대응은 원본 자료와 일치한다. 다만 이 자료는 직접 측정한 두께나 격자 내부의
연령별 면적 비율이 아니다. 그래서 화면 문구를 다음처럼 고쳤다.

- `해빙 연령` → `해빙 연령 추정`
- `2년차 이상 / 전체 해빙 범위` → `2년차 이상 격자 / 전체 해빙 격자`
- 그래프 최솟값 `18%` → `0%`
- `높이도 해빙 연령 표현 · 실제 두께 아님` 문구 유지

## 데이터 해석

NSIDC-0611은 해빙 이동 벡터를 이용해 얼음 입자를 매주 추적하는 방식으로
해빙 연령을 추정한다. 한 격자에 여러 연령의 입자가 있으면 가장 오래된
입자의 연령을 그 격자의 값으로 기록한다.

| 데이터값 | 공식 의미 | 작품 범례 |
|---:|---|---|
| 0 | 열린 바다 또는 해빙 농도 15% 미만 | 바다 |
| 1 | 0~1년, 1년차 얼음 | 0–1 |
| 2 | 1~2년, 2년차 얼음 | 1–2 |
| 3 | 2~3년, 3년차 얼음 | 2–3 |
| 4 | 3~4년, 4년차 얼음 | 3–4 |
| 5~16 | 4년 초과, 5년차 이상 | >4 |
| 20 | 육지 | 육지 |
| 21 | 연령을 계산하지 않은 해역 | 표시 제외 |

`여름을 견딘 얼음`은 2년차 이상, 즉 적어도 한 번의 여름철 최소 해빙 시기를
통과한 얼음을 뜻한다. NSIDC는 해빙 농도가 15% 이상으로 유지된 입자를
생존한 것으로 보고 다음 연령 구간으로 올린다.

## 핵심 지표 검증

화면 수치는 다음 식으로 계산한다.

```text
2년차 이상 격자 비율
= (연령값 2~16인 격자 수) / (연령값 1~16인 격자 수)
```

2013년 제11주는 `19.9245%`이며 화면의 `19.9%`는 소수점 첫째 자리
반올림값이다. 이 값은 동일 면적 EASE-Grid의 분류 격자 비율이다. 각 격자
내에서 2년차 이상 얼음이 차지하는 실제 면적 비율로 해석하면 안 된다.

그래프는 1984~2024년의 같은 제11주만 비교한다. 세로축을 0~50%로 고정해
잘린 축이 감소 폭을 과장하지 않게 했다. 이 기간 자료의 최댓값은 48.0%로
축 범위 안에 들어간다.

## 3D 표현 검증

색과 낮은 부조 높이는 모두 연령 구간 1~5에 대응한다. 높이는 시각적
구분을 위한 정규화 값이며 실제 해빙 두께가 아니다. 해빙 연령은 일반적으로
두께의 대리지표로 쓰일 수 있지만 두 값은 동일하지 않으므로, 영상에서
높이를 미터 단위로 표시하거나 두께 데이터라고 부르지 않는다.

## 최종 적용 조건

- 목업의 다운샘플링은 렌더링 해상도에만 쓰고 수치 계산에는 쓰지 않는다.
- 보간 프레임은 연도 사이의 시각적 전환일 뿐 관측값이나 예측값으로 설명하지 않는다.
- 해빙 감소를 해수면 상승량으로 환산하지 않는다.
- 특정 생물종 감소의 단일 원인으로 단정하지 않는다.
- 지도는 화면 점유율을 높이기 위해 외곽 일부가 프레임 경계에 걸리지만,
  오른쪽 아래 비율은 화면에 보이는 부분이 아니라 원본 전체 해빙 격자로 계산한다.

## 환경·생태 메시지

NOAA는 해빙을 해양 생물의 중요한 서식지이자 대기와 해양 사이의 열·수분
교환을 조절하는 경계로 설명한다. 따라서 다음 문장은 근거 범위 안에 있다.

> 해빙은 북극 생물이 살아가는 공간이자 바다와 대기를 잇는 경계다.  
> 오래된 얼음의 감소는 그 환경의 구조가 바뀌고 있음을 보여준다.

## 최종 영상 검증

| 항목 | 결과 |
|---|---|
| 파일 | `outputs/final/polar-memory-final-v6.mp4` |
| 형식 | H.264 MP4 |
| 해상도 | 1920×1080 |
| 프레임률 | 60fps |
| 재생 시간 | 44.5초 |
| 전체 프레임 | 2,670프레임 |
| 픽셀 형식 | yuv420p |
| 오디오 | 없음 |
| 파일 크기 | 23,144,385바이트 |
| SHA-256 | `7d65dd5789974f20e34b3bf82ce99df6345e62fad534c44a14bb8ddbf954084e` |

1984~2024년 41개 연도 지도와 연도 사이 중간 3D 상태 200장에 누락이 없고
전체 영상 디코딩이 완료되었다. 각 전환은 같은 격자의 블록 높이와 출현·소멸만
보간하며, 관측되지 않은 수평 이동 경로는 만들지 않는다. 색은 전환 중에도 다섯
해빙 연령 범주만 사용한다.

연도 지도와 중간 상태의 고정 바다·육지 시험 영역을 비교한 결과 평균 및 최대
RGB 차이가 모두 `0`이었다. V5에서 중간 상태의 육지 재질이 잘못 분기되며
배경 전체가 밝아지던 문제를 수정했고, V6에서는 배경 톤이 고정된다. 출력
프레임률은 60fps이며 인접한 3D 상태 사이를 8단계로 시간 보간한다.

시작·중간·마지막과 연도 전환 프레임에서 글자 잘림, 긴 반투명 잔상,
검은 화면과 패널형 장면이 없음을 확인했다. 마지막 비교는 같은 지도 좌표를
중앙 수직선으로 나누어 1984년과 2024년을 직접 대비한다. 화면 수치는 원본
전체 해빙 격자로 계산한 `summary.csv`와 일치한다.

## 참고 자료

1. NSIDC, *EASE-Grid Sea Ice Age, Version 4 User Guide*  
   https://nsidc.org/sites/default/files/documents/user-guide/nsidc-0611-v004-userguide.pdf
2. NSIDC, *EASE-Grid Sea Ice Age, Version 4*  
   https://nsidc.org/data/nsidc-0611/versions/4
3. NOAA, *Arctic Report Card 2024: Sea Ice*  
   https://arctic.noaa.gov/report-card/report-card-2024/sea-ice-2024/
